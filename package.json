{
  "name": "mrroot",
  "version": "1.0.0",
  "private": true,
  "description": "Personal AppSec blog. Static HTML/CSS/JS with Markdown-powered posts.",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "node scripts/serve.js",
    "dev": "npm run build && npm run serve"
  }
}
